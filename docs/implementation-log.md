# 実装状況と判断記録

対象: docs/tasks.md、要件定義書v1.14、会話内の追加指定。開始: 2026-09-09。

## 実装範囲

P0〜P4を実装済み。現在はv0.4.0。Pages公開・HTTPS設定に加え、基盤のRegistry登録完了後に通知登録・予約・取消・再送・SW受信・オフライン起動を実装。実機でのOS通知表示と実配送は別途確認する。

## 初期の画面指定（2026-09-09）

配色・書体はv0.1.1節の承認内容で更新済み。以下は初期実装時の記録。

- ヘッダにアイコンと `!=テンパリスト`、下段に NOT EQUAL TEMPALIST。
- 共通フッターに小さく版数と `© 2026 SIKUMI LAB`。
- 参考画像の余白を活かし、チェック項目を主役にする。青緑 #24665d、紙面 #f4f7f6、白 #ffffff、本文 #203633、境界 #d4e0dc、警告 #9b4c25 を初期トークンとする。ダーク配色も用意する。
- 具体的なGoogle Fontsの書体は未指定。選定まではゴシック系の標準フォールバックで表示し、読み込み先を差し替えられるようにする。

## 判断

- 新規フォルダで既存Gitリポジトリは存在しない。現在の専用フォルダに開発ブランチを持つGitを初期化する。
- 共有URLは `?t=` から `#t=` に変更する。フラグメントはHTTP送信されず、要件C-1「中身をサーバーに送らない」を満たすため。旧形式の配布実績はなく互換移行不要。[根拠](https://developer.mozilla.org/en-US/docs/Web/URI/Reference/Fragment)
- 業務データは `tempalist:data` に単一JSONスナップショットで保存する。別々のキーへ順次保存すると途中失敗で整合が崩れるため。通知用3キーは別管理し、バックアップに含めない。
- 容量警告はlocalStorageのキー・値のUTF-16概算で4MiBを目安に事前案内し、実際の保存成功・QuotaExceededErrorを最終判定とする。ブラウザ全体の容量推定でlocalStorageの残量を断定しない。[根拠](https://developer.mozilla.org/en-US/docs/Web/API/Storage_API/Storage_quotas_and_eviction_criteria)
- 共有URLの初期上限は最終URLで8000文字。一般的な保証値ではなくアプリ側の運用値とし、超過時は承認済みの共有JSONへ切り替える。
- 通知未提供段階では通知許可を要求せず、期限強調表示を提供する。通知UIは未提供と明示し、有効であると誤認させない。

## タスク進捗

| 範囲 | 状態 | 検証 |
| --- | --- | --- |
| T-01 基盤・版数 | ローカル実装済み。公開設定は未実施 | 構文・版数・識別子一致、配信物の生成 |
| T-02 共通UI | 実装済み | Chromeの375px/1280px、ダーク配色、動きを減らす設定 |
| T-03 保存 | 実装済み | 容量不足時の原状保持、破損JSON、別タブ競合 |
| T-04〜T-11 テンプレート・チェックリスト | 実装済み | 5種37項目、50項目の編集・並べ替え・自動スクロール、保存・確定・再開、業務ルールのテスト |
| T-12〜T-14 データ移動・容量管理 | 実装済み | 共有の下書き登録、HTML文字列の無害化、資格情報の除外、2回の追加取り込み、容量不足時のキャンセル・削除・再試行 |
| T-15〜T-20 通知 | アプリ連携は保留 | 共通基盤は発案者からデプロイ済みとの報告。アプリ連携・実機検証は後日 |
| T-21 日時 | ユニット検証済み | UTC/JST・月末・DST境界、日付8桁と09:00補完 |
| T-22 画面・書体 | A1改を反映・実画面の確認待ち | Zen Kaku Gothic Newを仮採用、標準ゴシックへフォールバック |

## 検証記録

- Vitest: 4ファイル・67テスト成功。通常実行と `npm run test:utc` の双方で確認。
- `npm run check`: JavaScript構文、package・manifest・画面版数の正典、アプリ識別子の一致を確認。
- `node scripts/check-artifacts.mjs`: 一時コピーのmanifest版数を意図的に変え、検証が失敗することを確認。配信物に開発ファイルが含まれず、現行ソースと一致することも確認。
- `npm run package:site`: `_site/`へアプリの静的ファイルを生成。公開操作なし。
- `scripts/browser-check.mjs`: 空から作成、項目・メモ追加、チェック保存と再読込、確定ロック、再開、画面幅を確認。
- `scripts/browser-regressions.mjs`: 50項目、編集・代替並べ替え・実ポインタードラッグ・端スクロールと停止、期限09:00、未チェック確定、容量不足時に入力を保持してキャンセル・整理・再試行、別タブの古い編集拒否、共有文字列のHTML無害化、draft登録、JSON書き出し・繰り返し追加取り込み、dark/reduced-motionを確認。
- 業務ルール・日時モジュールは独立レビュー済み。指摘なし。
- 最終レビューで見つかった保持期間変更キャンセル後の表示不一致と、書き戻し確認中の別タブ変更の上書きを修正。`scripts/browser-review-fixes.mjs`でキャンセル・Escape・設定の再変更、書き戻し元／先の変更を保護すること、通常の書き戻しと無関係な容量整理後の保存を確認。対象差分の再レビューで承認済み。
- `node scripts/check-serving.mjs`: `/`・`/list`の配信、存在しないアセットでサーバーが停止しないこと、docs・package・Git情報を配信しないことを確認。
- 最終の `npm run test:browser` は3スクリプトすべて成功。ドラッグ表示の終了直後に保存を検証していたテストの競合は、保存済み順序を待つようテストを修正して解消した。アプリの保存処理は変更していない。

## 確認用の画面

`node scripts/capture-preview.mjs` は隔離ブラウザで承認済みサンプルを使い、`artifacts/preview-home-mobile.png`、`preview-checklist-mobile.png`、`preview-home-desktop.png`、`preview-templates-desktop.png` を生成する。利用者の保存データを変更しない。375pxと1280pxでヘッダ・フッター・項目の折り返し・横方向のはみ出しがないことを確認した。

## 試用・次の確認

- A1改の実画面と長押し操作を確認する。フォントは候補からZen Kaku Gothic Newを仮採用したため、Noto Sans JPへの変更希望があれば調整する。
- iOS Safari／Android実機のタッチ・ドラッグ・ピッカーは未検証。Chromeのモバイル幅による検証とは区別する。
- 個人試用を数週間行った後、通知連携の検証を行う段階でP4へ進む。現版はPush、Service Worker、オフライン起動を提供しない。
- 本番用ドメイン・GitHub Pages・Registry・VAPID設定は未変更。

## v0.1.1 — 2026-09-10 配色・書体・並べ替え

- 発案者承認のA1改を反映。背景 #fcfaf9、白いカード、チェック済み行 #f7eeeb、アイコン・チェック・主要ボタンは共通の #f26450。明るいボタン上の文字は視認性のため濃色。OSのダーク設定でもライト配色を維持する。
- 日本語のアプリ名は字間を広げ、英語表記と幅を揃えた。候補からZen Kaku Gothic Newを仮採用し、400・500・700をGoogle Fontsからdisplay=swapで取得する。フォールバックは標準ゴシック系。
- 右端のつまみを約450ms長押しすると半透明カードが浮き上がり、指／ポインターに追従する。移動先の線と「ここに移動」を表示し、指を離した時点で保存する。短いタップ・成立前のスワイプでは開始しない。Escape・pointercancel・捕捉解除・画面離脱時は保存しない。端スクロールと⋯メニューの上下移動は継続する。
- テスト駆動で並べ替えの9ケースを追加。新仕様で旧実装が失敗することを確認してから実装し、合計5ファイル・76テストが成功。構文・版数整合チェックも成功。
- 既存3本のブラウザ検証はすべて成功。追加の `scripts/browser-appearance.mjs` はChromeのCDPタッチ入力で、短いタップ、長押し、カード追従、移動先表示、保存・再読込、チェック状態保持、キャンセル、本文の通常スクロールを確認した。OSダーク設定時のライト背景、reduced-motion、320/375/1280pxの横はみ出し、Google Fontsを遮断した新規コンテキストでの表示・チェック保存も成功。通常コンテキストではGoogle Fontsの実読み込みを確認。
- `npm run test:browser` に追加の画面検証を組み込んだ。`artifacts/preview-reorder-mobile.png` と `preview-font-fallback-mobile.png` に確認用画像を出力する。ブラウザエミュレーションとiOS／Android実機検証は区別し、実機での操作確認は未実施。
- GitHubの `https://github.com/gerupon-lgtm/tempalist` をoriginに登録。読み取りで参照未登録の空リポジトリを確認した。push・デプロイ・DNS設定は未実施。

- v0.1.1の `npm run package:site` と `node scripts/check-artifacts.mjs` が成功。版数不一致を拒否し、配信物が現行ソースと一致することを確認した。通常の4種のプレビュー画像もv0.1.1で再生成し、375px／1280pxで目視確認した。

## v0.1.2 — A1改画像との照合・カード全体の長押し（確認版）

- 発案者から実装と承認画像の配色差を指摘されたため画像を再確認。v0.1.1で茶色寄りにした背景・文字・罫線と黒文字のボタンが画像の印象から外れていた。画像上の画素も参照し、背景 #fffefe、本文 #142432、補助文字 #627283、罫線 #cfd9e3、淡いピンク #fcf3f4、コーラル #f5614c に補正。主要ボタンは画像と同じ白文字・19px太字に変更し、チェック済みラベルの取り消し線と減色も除いた。
- 発案者の試用希望に合わせ、チェック領域と右端の操作領域を除いたカード全体を450ms長押しして並べ替える形へ変更。右端に↑↓を常時表示し、⋯は編集・削除のみ。カード本文・余白が長押し対象で、チェック・ボタンにはドラッグを割り当てない。
- Touch Eventsで長押し成立後だけスクロールを抑止する。成立前の移動は通常スクロールを維持する。複数指・中断は保存しない。マウス／ペンはPointer Eventsを使う。
- 変更したテストが旧実装では失敗することを確認してから修正し、5ファイル83テスト成功。既存のブラウザ4本すべて成功。追加したタッチ検証でも、右端領域とチェック領域の長押しでドラッグしないこと、右端の上下ボタンで保存されることを確認した。
- 現在は配色と操作方式の発案者確認待ち。画面設計と起動案内に現状を記録し、正式な要件・タスクの一括更新は確認後とする。

- 文字の太さについての追加指摘を受け、v0.1.2で候補のNoto Sans JPへ変更。本文・補助文500、項目16px・500、主要ボタン19px・700・白文字とした。画像を元にしたフォントの調整も発案者の確認待ち。

## v0.1.3 — メニューを閉じる操作・長いメモ・書体比較

- カードの⋯メニューは外側クリック／タップ・Escapeで閉じ、同時に開くのは1つにした。隣のカードのボタンと重ならない配置に補正。
- 発案者の追加指示に従い、短いメモのカード内表示は維持。3行に収まらないメモだけ「続きを読む」を表示し、シングルタップで画面下部に全文を表示する。画面幅とフォント読み込みの変化で折り返しを再測定する。閉じる・外側・Escape・画面遷移で閉じ、長押し／ドラッグ直後は開かない。メモはHTMLとして解釈しない。
- `tests/card-interactions.test.js` を追加し、全6ファイル89テスト成功。`scripts/browser-card-interactions.mjs` をブラウザ検証へ追加。従来4本に加え、外側でのメニュー閉じ、単一メニュー、短いメモの維持、長いメモの全文・文字列安全性、キーボード、長押しとの区別、画面遷移時の片付け、画面幅変更時の省略再判定を確認。
- `npm run package:site` と配信物チェックが成功。公開操作なし。
- 書体を変更した意図は画像の文字の太さへ近づけるためで、比較目的ではなかったことを説明。`scripts/capture-font-comparison.mjs` で同じ配色・サイズ・太さの指定・データを表示したZen Kaku Gothic New／Noto Sans JPの実画面を並べる。成果物は `artifacts/font-comparison.html` と `.png`、個別画像は `font-zen.png`／`font-noto.png`。フォント選択は再確認待ちで、現アプリはNoto Sans JPを維持。

## v0.1.4 — ヘッダ字間・書体確定

発案者指定によりNoto Sans JPを正式採用。英語名の固定幅・均等割り付けをやめ、単語間を約1文字分に調整した。英語名の自然な幅を基準に日本語名の字間も調整。Chromeの320／375／1280pxで、英語・日本語の幅はいずれも159.72px、英語の単語間は10.30px（文字サイズ10.56px）、横はみ出しなしを確認。モバイルのヘッダ画像も目視確認した。静的配信物と版数の整合チェック成功。

## v0.1.5 — 一覧画面の作成ボタン位置を統一

リストだけに適用されていたモバイルの縦配置をテンプレート一覧にも適用し、見出し領域の最小高さ100px・作成ボタン幅156pxを共通化。Chromeの320／375／680／768／1280pxで、画面を切り替えても両ボタンのx・y・幅・高さが一致し、横はみ出しがないことを確認。モバイル2画面の画像を目視確認した。CSSのみの表示変更で、業務処理は変更していない。

## v0.1.6 — 並び順ロック・タブ復帰・モバイルの余白

- テンプレートで新規リストの並び順ロック初期値を設定し、リストへコピーする。リストでは独立してON/OFF切替可能。長押し・↑↓・保存層での順序変更を禁止し、チェック・編集・追加・削除は許可する。旧データと空から作成するリストはOFF。
- ロック初期値と状態は複製・共有・バックアップで保持。旧データの補完と不正値拒否、作成済みデータへの非伝播、確定／再開時の保持、書き戻し時の扱いを検証した。
- リストタブは直前のチェックリストへ戻る。一覧へ明示的に戻った場合と対象削除時は記録を解除する。同じブラウザタブ内の再読込でも維持する。既存テストで一覧へ戻る操作は明示的なホームリンクに置き換えた。
- モバイル一覧の100px固定見出し枠を廃止し、作成ボタン上の間隔を8pxにした。375pxではボタンのy座標が283.55pxから227.70pxへ約56px上がり、320／375／680／768／1280pxすべてで両画面のボタン位置が一致。スクリーンショットも目視確認。
- 追加指示により、並び順ロック前の余白を24px＋12pxから8px＋6pxへ縮小した。
- ユニットテスト7ファイル100件成功。ブラウザ検証は既存5本＋並び順ロック＋タブ復帰の全7本成功。静的配信物・版数チェック成功。公開操作なし。
# 2026-09-10 v0.1.7 Pages公開・通知接続の準備

- 発案者の依頼で `feat/tempalist-foundation` をGitHub Pagesの公開元に設定。UTCテスト・配信物検証後に `_site/` のみ配信するActionsを追加。カスタムドメインとHTTPS強制を設定し、初回デプロイ成功・HTTP 200を確認。
- 通知APIの公開鍵GETはHTTP 404 `APP_NOT_FOUND`。発案者が基盤側の別タスクで登録する方針を確認。必要な値と受け入れ条件は `deployment.md`。
- `src/notification/api.js` に固定v2パスへの公開鍵GETを実装。Cookie送信・redirectを無効化し、10秒タイムアウト、strict応答形式・鍵の形式検証、固定エラーメッセージを使用。業務データを引数にせず送信しない。
- 設定画面の明示操作から接続を確認できる。CSPはこのAPI Originのみ追加許可。端末登録・Outbox・予約・Service Worker・Push配送は未実装であり、接続確認成功だけでは通知を有効にしない。
- 依存なしのESM構成を維持。サブディレクトリの構文・配信物の比較も検証対象に追加。
- 検証: UTCユニット118件成功、ブラウザ検証8本成功、構文・版数・配信物検証成功。API未登録／CORS通信失敗／成功応答を模擬し、許可要求・業務データ変更がないことを確認。

## 2026-09-10 v0.2.0 通知連携とPWA

- 公開Originで公開鍵API 200と予約操作のCORS preflight 204を確認。開発Originは403で未許可。本番通知の確認は公開画面で行う。
- 6APIのrequest/responseをstrict検証し、端末登録・購読更新・予約作成／置換／取消・端末停止に対応。業務データを送らず、同じ送信操作は保存した冪等キーで再送する。
- `tempalist:notification`に端末・購読・対応表・Outbox・停止／購読更新操作を単一JSONで保存。通知用Web Lockで直列化し、別タブや再起動による二重予約を防ぐ。業務保存と通知通信は分離する。
- 期限なし／全オフセット削除／確定で通知をOFFにし、対応表を取消成功まで保持。送信済みの過去枠は再作成しない。通信断・429・500・503はバックオフ、永続的エラーは明示再試行待ち。
- SWは受信payloadを検証して固定文のみ表示し、固定ルートと検証済みIDで遷移する。アプリが対応表からリストを解決し、不在なら一覧へ戻る。外部URL・未知キー・別appId・不正UUID・余分な項目は受け付けない。
- オフライン用アプリシェル、PNGの192/512pxアイコン、iOSホーム画面追加案内、手動の新版適用を追加。ヘッダ下に1pxの薄いグレーの区切り線を反映。
- 検証: UTCユニット165件成功。既存ブラウザ検証8本と通知ブラウザ検証1本成功。Chromeへの模擬PushでSWからshowNotificationへの固定文の要求とリスト遷移を確認。本番基盤から実機への配送・OS通知表示は未確認。構文・版数・配信物検証成功。
- 実機での確認手順はnotification-verification.md。サービス側のコード・設定・既存あとキューの通知には変更を加えていない。

### v0.2.1 公開後の購読失敗案内

公開画面のAPI接続・SW登録は成功。隔離headless Chromeでは実Push購読が完了せず、端末登録・実配送は実機確認待ち。購読失敗の生のブラウザ例外を表示せず日本語案内に統一し、20秒タイムアウトで操作を解放する。失敗時は端末登録APIへ進まず、業務データを変更しない。追加2件を含むユニット検証を実施。

## 2026-09-10 v0.3.0 全体備考・メール用の記録

- ヘッダの既存下線に加え、モバイルのロゴ・名称とタブの間にも区切り線を追加。
- 項目下の備考欄を常時表示し、完了前後とも直接編集・保存可能にした。完了後も備考のみを更新でき、確定日時やチェック内容を維持。備考更新日時を独立して保持する。
- 未保存入力の画面切替・再描画での保持、終了時の警告、別タブの備考変更との競合検出を実装。完了確定・単体出力時は対象リストの備考を先に保存する。
- 未完了・完了の双方を、チェックリスト名の件名、コメント・全体備考・各日時入りの本文、JSON添付として出力できる。Web Share非対応時はJSON保存・コピー・手動添付・MIMEメールファイルを提供する。アプリは送信を自動実行しない。
- 単体記録JSONをPCで読み取り、またはアプリの追加取り込みで利用できる。時刻はUTCで保持し、メール本文は端末タイムゾーンとUTCを併記。端末時計の記録であり第三者署名ではないことを明記した。
- 検証：UTCユニット172件成功。既存9本＋追加1本の隔離ブラウザ検証成功。日本語EMLの件名・本文・JSON添付をPython標準emailパーサーでも確認。構文・版数・配信物検証成功。実際のメールアプリへの受け渡し・実送信は利用端末での確認が残る。
- 要件定義書v1.14、画面・データモデル・タスクとchecklist-records.mdに反映。新モジュールをSWキャッシュ対象へ追加し、版数を0.3.0に統一。

## 2026-09-10 v0.3.1 Androidインストール案内・更新の修正

- Androidのインストール案内が未実装で、更新ボタンも設定だけにあったことを確認。0.2.1を配信後に旧HTTPキャッシュを残して0.3.0へ切り替えると、0.3.0のCache Storageへ0.2.1のversion.jsが保存される経路を再現した。
- 全アプリファイルの取得にcache:reloadを指定し、SW・version.js・manifestの版が一致しなければ新版を有効にしない。入力を守るため無条件の自動適用は追加せず、新版案内と利用者の更新ボタンを全画面へ提供した。
- beforeinstallpromptの案内と明示操作によるインストール、設定の手動導入手順・常設更新確認を追加。未保存の備考や編集ダイアログがある場合は適用・再読み込みを保留する。
- 旧版のキャッシュ対象外の/update/に更新専用画面を用意。アプリの旧モジュールに依存せずWorkerを更新し、localStorageを削除しない。Pagesと専用ローカルサーバーの配信対象へ追加した。
- 検証：UTCユニット181件、既存10本＋PWAのブラウザ検証1本成功。0.2.1から更新・専用ページから復旧・HTTPキャッシュ回避・保存データ同一性・未保存備考の保護・オフライン起動を実Workerで確認。インストールイベントは模擬で確認。利用者のAndroidでの最終確認手順はpwa-update.md。

## 2026-09-10 v0.3.2 詳細見出しの可読性

利用者のAndroid画像で、タイトルの途中改行と「編集」の縦割れを確認。詳細見出しに専用レイアウトを追加し、説明文による横幅の圧迫を解消。タイトルを画面幅に応じた17〜22pxに調整し、編集ボタンを高さ44pxで固定した。Noto Sans JPの指定と配色は維持する。

検証：320〜1280pxの7幅で提示されたタイトルが1行、編集ボタンが44px、横はみ出しなしを確認。Google Fontsを遮断した場合も同じ条件で成功。長い日本語タイトルの折り返しも実画面で確認した。

日付・時刻入力をsrc/date-time-fields.jsに分離し、入力欄内にピッカー起動アイコンを統合。タップ時の全選択、年4桁・月2桁直後の `/` と時2桁直後の `:` の自動補完を追加した。手入力と標準ピッカーの同期、区切り文字の削除、入力検証、期限のUTC保存を維持する。

項目名も表示幅・文字数に応じて14〜16pxと字間0〜-0.02emで調整。CSPに合わせてインラインstyleを使わず、文字数の属性とCSSで実装。320／375／412／1280px、並び順ロックON/OFF、Google Fontsあり／なしで、短文のサイズ維持・長文の改行・横はみ出しなしを確認した。

検証：UTCユニット190件成功。既存ブラウザ検証11本と日時入力検証1本で、全選択からの置換、連続入力、区切りをまたぐBackspace、ピッカー起動、UTC保存・再編集・期限クリアを確認。端末OS固有のピッカー外観と実機キーボードは利用端末での確認が残る。

## 2026-09-10 v0.3.3 チェック済み項目の取り消し線

チェック状態に連動して項目名に1pxの取り消し線を付けるCSSを追加。チェック解除時は線も解除し、メモには適用しない。隔離Chromeでチェック・解除、再読み込み、完了後の表示、再開後の解除を確認した。

## 2026-09-10 v0.3.4 Androidのメール共有

利用者の「共有できませんでした。JSONを保存してメールに添付してください。」を隔離Chromeの実共有APIでも再現。canShareのtrue判定だけでは、Chromiumの共有処理時の拡張子・MIME制限を確認できていなかった。Android側の許可形式にも `.json` / application/json が含まれないことを確認した。

共有添付を同内容の `.json.txt` / text/plainへ変更し、画面に形式と再利用方法を表示。通常のJSON保存・EML添付は維持。取り込みのファイル選択に `.txt` を追加し、内容の検証は既存のJSONスキーマ検証を使う。ファイル共有非対応時の本文共有、キャンセル後のエラー解除・再試行も確認した。テンプレートのURL共有は引き続き利用でき、実行リストの記録URL共有は未実装であることを資料に明記。

検証：browser-checklist-record.mjsで形式拒否を模擬し、修正前の失敗・修正後の成功を確認。件名・本文・データの保持、共有失敗／キャンセル／再試行、本文共有、API非対応、共有テキストの取り込みを検証。実機でのメールアプリ受け渡し・送信は利用者の確認が残る。

## 2026-09-10 v0.3.5 通知設定で期限を直せない問題

iOSでの端末登録後にリスト通知をONにできないとの報告を受け、端末登録とは独立した通知時刻の検証条件を確認した。期限なしのリストで通知ON保存に失敗し、ダイアログ内で期限を修正できない問題を再現。利用者の実際のエラー文・起動方法は回答待ちで、iOS固有の原因はまだ確定していない。

通知ダイアログに既存の日付・時刻入力とピッカーを組み込み、各通知時刻と予約可能件数を表示。期限未設定・タイミング未選択・全時刻経過を別の文言で説明する。期限・通知設定をまとめて保存し、保存直前にも条件を検証する。別タブによる期限・通知設定の変更は競合として扱い、チェック項目等は最新状態を維持する。

検証：browser-notifications.mjsを端末登録後にリスト設定する流れへ拡張し、修正前の失敗を確認。修正後は期限未設定・期限まで30分・タイミング未選択から同じ画面で修正して通知ON保存し、模擬APIへ2件の予約が作成されることを確認。既存の模擬Push、再読み込み、取消、端末停止も成功。iOS実機の設定・配送の成否は引き続き確認が必要。

## 2026-09-10 v0.3.6 iOS購読データ・ピッカー互換性

画像でv0.3.5・ホーム画面起動・未来の通知時刻を確認。「通知の送信内容を確認してください」はアプリ側のAPI送信前検証で発生しており、期限条件のエラーではなかった。Safari形式でexpirationTimeを省略した購読データを用い、同じ登録失敗を再現した。ブラウザ購読の登録・現在値取得の両方でexpirationTimeの未設定をnullへ正規化し、基盤側の契約やAPI検証は維持する。登録エラーを通知ステータスにも残す。

iOSでdate/timeのshowPickerが無反応でも例外を出さず、従来のcatchによる代替処理へ進まない問題に対応。iOSとiPadデスクトップUAではアイコン上に標準入力の直接タップ領域を配置する。手入力値の引継ぎと変更結果の反映は共通化し、通常ブラウザの既存動作も維持する。

回帰テストで修正前の登録失敗・ピッカー代替不足・登録失敗メッセージ消失を確認。修正後はSafari形式の登録から通知予約・同期・取消まで、iOS向けアイコンのタップ先・フォーカス、手入力・UTC保存を確認した。iOSのOS画面と実通知配送は模擬検証とは区別し、実機確認が残る。

## v0.3.7 端末登録の継続と表示（2026-09-10）

通常更新では端末登録を維持する。あとキューの保存済み資格情報を再利用する処理と比較し、テンパリストにも版数による登録削除がないことを確認した。購読のJSONプロパティ順が違うだけで再設定を促す問題と、一時的な購読取得失敗を再設定扱いにする問題を再現し修正。設定画面は登録済み状態と同期状況を併記する。実機で報告された次回未登録表示の直接原因は未確定。

検証: UTC単体200件成功。通常・iOS形式UAの隔離Chromeで登録、予約、再起動後の登録維持、取消を確認。実SWの旧版からの更新・次版更新・専用更新ページを経ても通知用localStorageが変わらないことを確認した。iOSの実Push購読・配送継続は実機確認が必要。

## v0.3.8 証跡メールの項目チェック日時（2026-09-10）

依頼: 証跡メールに各項目をチェックした時刻を残す。ChecklistItem.checkedAtを追加し、チェック操作で保存、解除時はnull、再チェック時は新しい日時にする。メール本文・共有データ・JSON・EML・バックアップと再取り込みで保持する。旧データの日時は推測せず、チェック済みでも日時がなければ「日時未記録」と表示する。仕様はdata-model.mdとchecklist-records.mdに反映。

検証: UTC単体207件成功。隔離Chromeで実際のチェック日時が未完了メール本文・共有JSON・保存JSONに入り、完了後の備考追記と再読み込み後も保持されることを確認。EML本文・添付JSONの復号、旧データ互換、解除・再チェック、書き戻し、再取り込みは単体テストで確認。配信物・版数検証も成功。

## v0.4.0 あとキュー起動URL連携（2026-09-11）

選択した予定を項目として渡す一方向URL連携を実装。UTF-8/Base64urlの #create= を受信し、タイトル変更可能なプレビューから保存する。初稿のパラメータ名・URL形式を維持。schemaVersion 1・kind checklist-create・source atoqueue・requestId・title・items（sourceTaskId/label/note）を検証し、URL8000文字上限を両側に適用する。

同じ連携IDは保存直前に再照合し、作成済みなら既存リストへ移動する。連携ID・元の予定IDを保存し、チェック・編集・記録出力で保持。コピー取り込みは直接受信の重複判定から除く。未保存入力がある画面へURLが来た場合は受信を中断して入力を保持する。既存のテンプレート共有と通知起動は維持する。

Androidでの連携を優先し、iOSは実機で起動先と保存領域を確認する。操作が成立しない、または煩雑になる場合は連携のみAndroid限定とする。OSのPWA起動先とあとキュー実画面の送信動作は自動テストとは別の確認事項。あとキュー側の担当向けにatoqueue-checklist-link.mdの生成例とscripts/print-checklist-link.mjsを用意した。

検証: UTC単体215件、ブラウザ検証16本、構文・版数・配信物検証が成功。ブラウザ全体検証は保存中キャンセル対策後に再実行済み。2タブ同時作成、編集中の受信、Web Locks待機中のキャンセル・Escape・別リンク遮断、保存失敗後の再操作を確認。規約レビューは指摘なし、仕様レビューの保存待ち中キャンセル問題は修正・回帰テスト・再レビューで解消。iOS形式UAのChrome検証はOSによるPWA起動や保存領域共有の実証ではない。

### 実機報告（2026-09-12）

利用者よりAndroidの連携成功を確認。iOSホーム画面のあとキューから開いたテンパリストにはリストが蓄積するが、普段のホーム画面・ブラウザのテンパリストには現れない。ブラウザのあとキューから送った場合は、ブラウザのテンパリストで保存を維持できるとの報告。iOSでパラメータ受信は成功しても、PWAの保存先一致という受入条件は満たしていない。詳細と送信側ソースの確認結果をatoqueue-checklist-link.mdの12章に記載。資料のみ更新し、版数・アプリの動作・保存データは変更していない。
